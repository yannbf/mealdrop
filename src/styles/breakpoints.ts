export const viewports = {
  xSmall: 400,
  small: 640,
  medium: 768,
  large: 1024,
  xLarge: 1280,
}

export const breakpoints = {
  XS: `(min-width: ${viewports.xSmall}px)`,
  S: `(min-width: ${viewports.small}px)`,
  M: `(min-width: ${viewports.medium}px)`,
  L: `(min-width: ${viewports.large}px)`,
  XL: `(min-width: ${viewports.xLarge}px)`,
}
