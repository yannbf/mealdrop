interface:
  display_name: 'Chromatic CI Setup'
  short_description: 'Configure CI pipelines to run Chromatic visual tests'
  default_prompt: 'Use $chromatic-setup-ci to detect the CI provider and package manager, fetch the current Chromatic docs for that provider, and generate the CI configuration.'

policy:
  allow_implicit_invocation: false
